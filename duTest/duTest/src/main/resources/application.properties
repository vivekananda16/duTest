spring.application.name=duTest
spring.datasource.url=jdbc:mysql://localhost:3306/du_test?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=papulit
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA Settings
# 'update' automatically creates/updates database tables based on your Entity classes
spring.jpa.hibernate.ddl-auto=update

# Shows the SQL queries Hibernate runs in your console (great for debugging)
spring.jpa.show-sql=true

# Specifies the exact MySQL version dialect (MySQL8Dialect is standard for modern MySQL)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
